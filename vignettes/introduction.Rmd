---
title: "Introduction à waterQualSDM"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Introduction à waterQualSDM}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  warning = FALSE,
  message = FALSE,
  fig.width = 8,
  fig.height = 6
)
devtools::load_all()
library(sf)
library(terra)
```

## Introduction

`waterQualSDM` est un package R pour analyser et modéliser la qualité du sol
dans les bassins versants agricoles au Maroc. Il permet de :

- Importer et nettoyer les données environnementales réelles
- Calculer des variables environnementales spatiales
- Prédire le **Soil Organic Carbon (SOC)** avec Random Forest (R² = 0.82)
- Cartographier les zones vulnérables
- Générer des recommandations de gestion durable

### Données utilisées

- **Variable cible** : Soil Organic Carbon (SOC) depuis SoilGrids (0-5cm, g/kg)
- **Occupation du sol** : ESA World Cover
- **Topographie** : SRTM 30s (altitude et pente)
- **Précipitations** : WorldClim 2.5min
- **Zone d'étude** : Maroc incluant le Sahara Occidental

---

## Étape 1 — Import des données

### Données qualité du sol

```{r}
file_wq <- system.file("extdata", "water_quality.csv", 
                        package = "waterQualSDM")
wq_data <- import_water_quality_data(file_wq)
head(wq_data$dataframe)
```

### Bassins versants

```{r}
file_ws <- system.file("extdata", "watersheds.geojson", 
                        package = "waterQualSDM")
watershed <- import_watershed(file_ws)
print(watershed)
```

### Modèle Numérique de Terrain

```{r}
file_dem <- system.file("extdata", "dem.tif", 
                         package = "waterQualSDM")
dem <- load_dem(file_dem)
print(dem)
```

### Occupation du sol

```{r}
file_lu <- system.file("extdata", "landuse.tif", 
                        package = "waterQualSDM")
landuse <- import_landuse_data(file_lu, reclassify = FALSE)
print(landuse)
```

---

## Étape 2 — Variables environnementales

### Variables hydrologiques

```{r}
hydro <- calculate_hydrological_variables(dem, watershed = watershed)
print(hydro$raster)
```

### Variables moyennes par bassin versant

```{r}
print(hydro$basin_vars)
```

### Extraction des features aux stations

```{r}
features <- extract_environmental_features(
  water_quality = wq_data$sf,
  landuse       = landuse,
  hydro         = hydro
)

# Garder les variables pertinentes
features_spatial <- features[, c("soc", "landuse", "slope", 
                                  "altitude", "precipitation")]
features_spatial <- features_spatial[, !duplicated(names(features_spatial))]
head(features_spatial)
```

---

## Étape 3 — Prétraitement des données

```{r}
data_prep <- preprocess_data(
  df        = features_spatial,
  target    = "soc",
  test_size = 0.2,
  normalize = TRUE
)

cat("Taille train :", nrow(data_prep$train), "\n")
cat("Taille test  :", nrow(data_prep$test),  "\n")
cat("Variables    :", paste(data_prep$feature_names, collapse = ", "), "\n")
```

---

## Étape 4 — Modélisation Random Forest

```{r}
model_result <- train_rf_model(
  data_prep = data_prep,
  target    = "soc",
  ntrees    = 500,
  tune      = TRUE
)
```

### Importance des variables

```{r}
print(model_result$importance)
```

---

## Étape 5 — Évaluation du modèle

```{r}
eval_result <- evaluate_model(model_result, data_prep, target = "soc")
knitr::kable(eval_result$metrics, caption = "Métriques de performance")
```

### Observed vs Predicted

```{r}
print(eval_result$plot_obs_pred)
```

### Graphique des résidus

```{r}
print(eval_result$plot_residuals)
```

### Importance des variables

```{r}
print(eval_result$plot_importance)
```

---

## Étape 6 — Prédiction spatiale du SOC

```{r}
soc_map <- predict_water_quality(
  model_result = model_result,
  landuse      = landuse,
  hydro        = hydro$raster
)
print(soc_map)
```

---

## Étape 7 — Indice de vulnérabilité

```{r}
vuln_map <- calculate_vulnerability_index(
  nitrates_raster = soc_map,
  landuse         = landuse,
  hydro           = hydro$raster
)
print(vuln_map)
```

---

## Étape 8 — Résumé par bassin versant

```{r}
summary_df <- summarize_watersheds(
  watershed       = watershed,
  nitrates_raster = soc_map,
  vuln_raster     = vuln_map,
  landuse         = landuse
)
knitr::kable(summary_df, caption = "Résumé par bassin versant")
```

---

## Étape 9 — Recommandations de gestion

```{r}
recommendations <- generate_recommendations(summary_df)
knitr::kable(
  recommendations[, c("NAME_1", "risk_class", "recommandations")],
  caption = "Recommandations par bassin versant"
)
```

---

## Étape 10 — Cartographie

### Carte du Soil Organic Carbon prédit

```{r}
cartes <- plot_water_quality_map(
  nitrates_raster = soc_map,
  vuln_raster     = vuln_map,
  watershed       = watershed
)
print(cartes$carte_nitrates)
```

### Carte de vulnérabilité au SOC

```{r}
print(cartes$carte_vulnerabilite)
```

### Carte des bassins versants

```{r}
print(cartes$carte_bassins)
```

---

## Conclusion

Ce pipeline complet permet de :

1. **Importer** des données environnementales réelles (SoilGrids, ESA, SRTM, WorldClim)
2. **Calculer** des variables environnementales spatiales
3. **Modéliser** le Soil Organic Carbon avec Random Forest (R² = 0.82)
4. **Prédire** spatialement la qualité du sol au Maroc
5. **Identifier** les zones vulnérables
6. **Générer** des recommandations de gestion durable
